node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.vercel
.env*.local
.env*

# Generated by scripts/setup-mediapipe.mjs from node_modules and
# the MediaPipe model CDN. Never hand-edited, never committed:
# postinstall regenerates it on every install/deploy so it always
# matches the pinned @mediapipe/tasks-vision version.
public/mediapipe/
